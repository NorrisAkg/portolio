DATABASE_URL="postgresql://postgres:password@localhost:5432/portfolio?schema=public"
JWT_SECRET="generate_a_random_jwt_secret_key"

# Initial Admin Credentials (Optional, falls back to default values during seeding if not set)
ADMIN_EMAIL="admin@portfolio.local"
ADMIN_PASSWORD="password123"

# SMTP Configuration (Optional, relayer logs to console in development if not configured)
SMTP_HOST="smtp.mailtrap.io"
SMTP_PORT=2525
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@votredomaine.com"
SMTP_TO="norris@votredomaine.com"
